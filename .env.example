# Formular-Anbindung (siehe UMBAU-BRIEFING.md, Abschnitt 5a)
# Leer lassen = Testbetrieb (kein Netzwerkaufruf, nur Konsolen-Log).
# Gesetzt = echter POST-Request an diesen Endpunkt (FormData, inkl. Fotos).
#
# Zum lokalen Verwenden diese Datei nach .env kopieren (.env ist in
# .gitignore und wird nicht committet).
PUBLIC_FORM_ENDPOINT=
