---
import { brand } from '../config/brand';

const base = import.meta.env.BASE_URL;
---
<img src={`${base}${brand.logo.src}`} alt={`${brand.name}.at – ${brand.tagline}`} width="130" height="48" class="logo-img">
